node_modules/
dist/
*.png
*.pdf
.DS_Store
PLAN-CHROMIUM-STEALTH.md

# npm token (never commit)
.npm-token
*.token

# experimental local build (project-scoped, not published)
dist-exp/

# npm pack artifacts — never commit these
*.tgz
